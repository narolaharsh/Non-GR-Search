It is used to generate single detector triggers.


--psd-file : We are not using this option right now. The script estimates PSD during the run.

--injection-window : Filter's only in the window around the end-time of the injections. It can speed up the search.   


The structure of the file used in inspiral-workflow output page and output of trigger.sh file is different, pycbc_page_foundmissed executable can't be used to generate the same kind of plots. 